[defaults]
inventory = inventory.yml
# Lets host_vars override a single nested key of a role's default dict
# (e.g. sources_repos.common.ubuntu.suites) without having to restate the
# whole dict.
hash_behaviour = merge
